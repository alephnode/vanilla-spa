{
  "name": "vanilla-spa",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "parcel build index.html && cp now.json dist/now.json && cd dist && now"
  },
  "dependencies": {
    "lit-html": "^1.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0-0",
    "@babel/plugin-transform-runtime": "^7.2.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "parcel-bundler": "^1.11.0"
  }
}
